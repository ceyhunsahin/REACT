import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(<App/>, document.getElementById('root'))

//! Parent Component (index.js) uses render() function to render web app. This takes 2 arguments
//* 1. the components or element to be rendered
//* 2. Where the web page renders
